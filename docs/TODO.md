# TODO

## Pending
- [ ] Build UTC→IST timezone converter (utils/timezone.go) — required before any calculations/signal generation. All DB timestamps are UTC, must convert to IST at consumption time.
